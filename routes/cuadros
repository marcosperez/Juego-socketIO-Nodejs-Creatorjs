var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res) {
  res.render('cuadros', { title: 'Juego Magico' ,estadoHome:"",estadoJuego:"",estadoCuadros:"active",estadoNaves:""});
});

module.exports = router;
